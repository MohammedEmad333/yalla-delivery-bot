# يلا ديلفري — بوت واتساب (Yalla Delivery WhatsApp Bot) 🛵

بوت واتساب تفاعلي مستقل لاستقبال طلبات التوصيل آلياً (طعام 🍔 / طرود 📦 / بقالة 🛒)،
مبني على Node.js و [Baileys](https://github.com/WhiskeySockets/Baileys).

## المزايا
- تدفق محادثة تفاعلي متعدد الخدمات مع آلة حالات لكل عميل.
- يجمع: الاسم، المصدر، التفاصيل، عنوان التسليم، الجوال، طريقة الدفع، وقت التوصيل.
- يحثّ العميل على استخدام التطبيق (أندرويد/تطبيق الويب).
- يحفظ الطلبات محلياً في `orders.json` (بلا أي API خارجي).
- ربط برمز اقتران (Pairing Code) أو QR، مع إعادة اتصال تلقائية وحفظ الجلسة.
- سيرفر Express لإبقاء الاستضافة نشطة + عرض QR/الطلبات على الويب.
- 🤖 **مساعد ذكي مجاني (Google Gemini):** يجيب على أسئلة العملاء الحرّة بلغة
  طبيعية (أسعار، مناطق، كيفية الطلب) ويوجّههم لإنشاء طلب — دون التأثير على تدفق
  الطلب. يعمل فقط عند ضبط `GEMINI_API_KEY`، ويعود تلقائياً لرسالة الترحيب إذا
  تعذّر أو انتهت الحصة المجانية.

## المساعد الذكي (اختياري ومجاني)
1. احصل على مفتاح مجاني من [Google AI Studio](https://aistudio.google.com/apikey) (بدون بطاقة ائتمان).
2. أضِف `GEMINI_API_KEY=المفتاح` في ملف `.env` أو في متغيّرات بيئة الاستضافة.
3. (اختياري) عدّل `GEMINI_MODEL` أو `AI_MEMORY_TURNS`، أو اضبط `AI_ENABLED=false` لتعطيله.

الحد المجاني لـ Gemini سخيّ (يكفي بوت توصيل عادةً). أي كلام حر يكتبه العميل خارج
الأوامر المعروفة يُحال للمساعد للإجابة، ويبقى تدفق الطلب التفاعلي كما هو.

## التشغيل محلياً
```bash
npm install
npm start
```

### الربط بالواتساب
الطريقة الافتراضية **رمز اقتران** لرقم الأعمال (`BUSINESS_NUMBER`):
1. شغّل `npm start` وسيظهر رمز من 8 خانات في الطرفية.
2. على هاتف رقم الأعمال: واتساب ← **الأجهزة المرتبطة** ← **ربط جهاز** ← **ربط برقم الهاتف بدلاً من ذلك** ← أدخل الرمز.

للربط بمسح QR بدلاً من ذلك اضبط `USE_PAIRING_CODE=false` وامسح الرمز من الطرفية أو من `http://localhost:3000/qr`.

## الإعدادات
انسخ `.env.example` إلى `.env` وعدّل القيم (الأرقام، روابط التطبيق...).

## المسارات
- `/` حالة الخدمة — `/health` فحص صحة — `/qr` عرض QR — `/orders` عرض الطلبات.

## النشر
- **Oracle Cloud (Always Free) — موصى به للتشغيل 24/7:** دليل كامل خطوة بخطوة مع
  systemd وإعادة تشغيل تلقائية وجلسة دائمة في [`DEPLOY-ORACLE.md`](DEPLOY-ORACLE.md).
- Render أو Alwaysdata: جاهز للرفع (Build: `npm install` / Start: `npm start`).

> ملاحظة: على الاستضافات ذات القرص المؤقت قد يُمسح `auth_info` و`orders.json` عند إعادة النشر — استخدم قرصاً دائماً للحفاظ على الجلسة. على Oracle القرص دائم افتراضياً.
