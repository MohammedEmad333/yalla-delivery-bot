# يلا ديلفري — بوت واتساب (Yalla Delivery WhatsApp Bot) 🛵

بوت واتساب لخدمة عملاء **Yalla Delivery** مبني على Node.js و
[Baileys](https://github.com/WhiskeySockets/Baileys).

> **مهم:** إنشاء طلبات التوصيل مباشرةً عبر واتساب غير متاح. جميع الطلبات تُنشأ من
> تطبيق Yalla Delivery فقط.

## المزايا
- يرسل روابط تطبيق يلا ديلفري للعميل ويحوّل أي طلب جديد إلى التطبيق.
- يجيب عن الأسئلة العامة المتعلقة بالأسعار والمناطق.
- يوفّر معلومات التواصل مع الدعم.
- لا يجمع الاسم أو العنوان أو تفاصيل الشحنة أو بيانات الدفع لإنشاء طلب.
- لا ينشئ طلبات من واتساب ولا يحفظها محليًا؛ ويمكنه قراءة سياق دعم محدود من Yalla API عند تفعيل التكامل الآمن.
- ربط برمز اقتران (Pairing Code) أو QR، مع إعادة اتصال تلقائية وحفظ الجلسة.
- سيرفر Express لإبقاء الاستضافة نشطة + `/health` و`/qr` و`/ai-status`.
- 🤖 مساعد ذكي عبر Google Gemini أو Groq مع ذاكرة محادثة قصيرة، حماية من إساءة الاستخدام،
  تحويل تلقائي للمزوّد الاحتياطي عند توفره، وتعليمات صريحة بعدم إنشاء الطلبات عبر واتساب.
- تنظيف تلقائي للجلسات الخاملة حتى تبقى ذاكرة الخدمة مستقرة مع التشغيل الطويل.
- **تدخل بشري تلقائي:** إذا ردّ صاحب حساب واتساب يدوياً على عميل، يتوقف البوت والـAI عن الرد على ذلك العميل فقط، ويستمران مع باقي العملاء. يعود الرد الآلي تلقائياً بعد مدة قابلة للضبط عبر `HUMAN_TAKEOVER_MIN` (الافتراضي 30 دقيقة).
- **تصعيد ذكي:** طلب الموظف/الإنسان صراحة أو تكرار مشكلة الدعم يحوّل المحادثة للتدخل البشري تلقائياً، مع حفظ سبب واضح مثل "مشكلة رمز التحقق" أو "طلب التحدث مع موظف". يمكن ضبط حد التكرار عبر `SMART_ESCALATION_REPEAT`.
- **استئناف البوت قبل انتهاء المهلة:** من نفس محادثة العميل يمكن إرسال `/بوت` أو `/resume` أثناء Human Takeover لإعادة الرد الآلي فوراً. هذه الأوامر تُعالج قبل حظر الردود أثناء التدخل، فلا تصل للـAI بالخطأ.
- **حلول أعطال شائعة:** ردود موثوقة لمشاكل تسجيل الدخول، رمز التحقق، تعطل التطبيق، الدفع/المحفظة، الطلب المتأخر، والموقع.
- **إحصائيات تشغيلية بدون حفظ محتوى الرسائل:** عدد الرسائل، ردود AI، الدعم، التصعيدات، وروابط التطبيق.
- **سياق حي من Yalla Backend:** عند ضبط توكن الربط، يقرأ البوت آخر طلب ورصيد المحفظة المتاح للحساب المرتبط بنفس رقم واتساب، عبر endpoint قراءة فقط لا يعيد العنوان أو أرقام الهواتف أو رموز التسليم أو بيانات الدفع.
- **Intent detection + handoff summary:** يصنّف طلبات حالة الطلب/المحفظة/الدعم، ويحفظ ملخصًا منقحًا لآخر الرسائل ليظهر للإدارة في `/محادثات` عند التحويل البشري.
- يمكن للعميل كتابة `/reset` أو **ابدأ من جديد** لمسح سياق المحادثة والبدء من جديد.

## تجربة العميل
القائمة الرئيسية:
- **1** فتح/تحميل التطبيق.
- **2** الاستفسار عن الأسعار والمناطق.
- **3** التواصل مع الدعم.

كما أن كلمات مثل **طلب**، **طلب جديد**، **توصيل** و**order** تعيد رابط التطبيق بدل
بدء تدفق طلب داخل واتساب.

## المساعد الذكي
يدعم مزوّدَين — يكفي مفتاح واحد:

**Google Gemini**
1. احصل على مفتاح من [Google AI Studio](https://aistudio.google.com/apikey).
2. أضف `GEMINI_API_KEY` في `.env`.

**Groq**
1. احصل على مفتاح من [Groq Console](https://console.groq.com/keys).
2. أضف `GROQ_API_KEY` في `.env`.

يمكن اختيار المزوّد عبر `AI_PROVIDER=auto|gemini|groq`، وضبط الذاكرة وحد المعدل
عبر `AI_MEMORY_TURNS` و`AI_RATE_MAX`. ويمكن ضبط `AI_MAX_INPUT_CHARS` للحد
الأقصى لطول السؤال و`AI_SESSION_TTL_MIN` لمدة الاحتفاظ بجلسة العميل الخاملة.

### أوامر الإدارة
- `/حالة` أو `/ai`: حالة المساعد وفحص حي للمزوّد.
- `/stats` أو `/إحصائيات`: إحصائيات التشغيل الحالية.
- `/محادثات`: المحادثات الموجودة حالياً تحت التدخل البشري مع سبب التحويل والوقت المتبقي.
- داخل محادثة العميل ومن حساب الأعمال: `/بوت` أو `/resume` لإعادة تشغيل الرد الآلي فوراً قبل انتهاء مهلة التدخل.
- HTTP: `GET /ai-status` يعرض حالة AI وإحصائيات البوت بعد التحقق بـ `ADMIN_HTTP_TOKEN`.
- الفحص الافتراضي في `/ai-status` يستخدم الحالة المخبأة ولا يستهلك حصة AI؛ أضف `?live=1` لفحص حي متعمّد.

> الإحصائيات التشغيلية تبدأ من آخر تشغيل للخدمة، ولا تحفظ نصوص رسائل العملاء.

## التشغيل محلياً
```bash
npm ci
npm test
npm start
```

### الربط بالواتساب
الطريقة الافتراضية رمز اقتران لرقم الأعمال:
1. شغّل `npm start`.
2. واتساب ← الأجهزة المرتبطة ← ربط جهاز ← ربط برقم الهاتف بدلاً من ذلك.
3. أدخل الرمز الظاهر في الطرفية.

للربط عبر QR اضبط `USE_PAIRING_CODE=false`.

## الإعدادات
```bash
cp .env.example .env
```

عدّل رقم الأعمال، رقم الدعم، وروابط التطبيق، ثم أضف مفتاح AI عند الحاجة.

### ربط بيانات الطلب والمحفظة

في Backend Yalla اضبط قيمة عشوائية طويلة:

```env
BOT_API_TOKEN=<shared-secret>
```

وفي خادم البوت استخدم نفس القيمة:

```env
YALLA_API_BASE_URL=https://api.yalladelivery.org
YALLA_BOT_API_TOKEN=<same-shared-secret>
YALLA_API_TIMEOUT_MS=5000
```

بعدها يستطيع العميل سؤال البوت مثل **"وين طلبي؟"** أو **"كم رصيدي؟"** ويحصل على بيانات مباشرة من النظام. إذا كان الربط غير مضبوط أو الـAPI غير متاح، يعود البوت تلقائيًا للسلوك الآمن السابق بدون تخمين.

## المسارات
- `/` حالة الخدمة العامة بدون أسرار.
- `/health` فحص الصحة.
- `/qr` عرض QR محليًا بدون إرسال الرمز لخدمة خارجية، ومحمي بـ `ADMIN_HTTP_TOKEN`.
- `/ai-status` تشخيص المساعد الذكي، ومحمي بـ `ADMIN_HTTP_TOKEN`.
- استخدم ترويسة `Authorization: Bearer <token>` أو `x-admin-token: <token>`.

## النشر
راجع [DEPLOY-ORACLE.md](DEPLOY-ORACLE.md) لتشغيل البوت 24/7 عبر systemd على Oracle Cloud.

> جلسة واتساب محفوظة في `auth_info/` وتبقى بعد إعادة تشغيل الخدمة.

## الأمان وCI

- اختبارات آلية عبر `npm test`.
- فحص syntax عبر `npm run check`.
- GitHub Actions يعمل على `main` وكل Pull Request باستخدام Node.js 22.
- `/qr` و`/ai-status` غير متاحين بدون `ADMIN_HTTP_TOKEN`.
- نصوص المستخدم تُنقح من أرقام/بريد/رموز تحقق شائعة قبل إرسالها لمزوّد AI.
- تحديثات Dependabot الأسبوعية مقصورة افتراضياً على patch/minor.

## بنية الكود

- `index.js` — تشغيل WhatsApp وتوجيه المحادثات.
- `src/security.js` — مطابقة أرقام الإدارة، حماية HTTP، وتنقيح البيانات الحساسة قبل AI.
- `src/http.js` — Express health/admin endpoints وQR المحلي.
- `test/bot.test.js` — اختبارات السلوك والأمان.
